
name: Post to Next

on:
  push:
    paths:
      - 'docs/all/*.json'

jobs:
  post:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: technote-space/get-diff-action@v6
        with:
          PATTERNS: '*.json'
          RELATIVE: 'docs/all'
        env:
          API_SECRET_KEY: ${{ secrets.API_SECRET_KEY }}
      - run: |
          echo ${{ env.GIT_DIFF }}  | cut -d. -f1
          curl -X POST -H "Content-Type: application/json" -d "{\"id\": \"${{ env.GIT_DIFF }} | cut -d. -f1\"}" https://subject.kgu-syllabus.com/?secret=${{ env.API_SECRET_KEY }}  | jq .jobs
        id: extract
      
